# 2026-09-05 Upgrade Calcit 0.13.77 / 升级 Calcit 0.13.77

- Align the embedded Rust runtime, Snapshot toolchain metadata, and exact JavaScript procs package on Calcit 0.13.77.
- Refresh the Cargo and Yarn lockfiles only for the aligned runtime/toolchain dependency graphs.
- Narrow the browser `document.visibilityState` value to `String` at its existing trusted JavaScript boundary, matching the storage-value adapter already used in the same entry and clearing the only project-local 0.13.77 preprocessing warning.
- Pin every workflow action to an exact readable release tag, disable checkout credential persistence, grant only read access to repository contents, and replace the obsolete Node 20 Jetli installer with the checksum-verifying Taiki installer pinned to wasm-pack 0.15.0.
- Preserve the existing nightly Rust choice, WASM/JavaScript build gates, and main-only deployment destination and secret contract.
- Upgrade the published Respo/Reel dependency roots to their migrated releases so Calcit 0.13.77 can generate JavaScript without dependency type warnings. Caps keeps normal highest-version resolution until every published leaf package has aligned its requested Respo versions.

- 将内嵌 Rust runtime、Snapshot 工具链元数据和精确 JavaScript procs 包统一到 Calcit 0.13.77。
- 仅针对对齐后的 runtime/toolchain 依赖图刷新 Cargo 与 Yarn 锁文件。
- 在现有可信 JavaScript 边界把浏览器 `document.visibilityState` 值收窄为 `String`，与同一 entry 已使用的 storage 值适配方式保持一致，并清除唯一一条项目本地 0.13.77 预处理警告。
- 将所有 workflow Action 固定到精确且可读的 release tag，禁用 checkout 凭据持久化，仅授予仓库内容读取权限，并用校验 checksum 的 Taiki installer 替换已过时的 Node 20 Jetli installer，固定安装 wasm-pack 0.15.0。
- 保持现有 nightly Rust 选择、WASM/JavaScript 构建门禁，以及仅 main 分支部署的目标与密钥契约不变。
- 将已发布的 Respo/Reel 根依赖升级到完成迁移的版本，使 Calcit 0.13.77 能够在没有依赖类型告警的情况下生成 JavaScript。在所有已发布的叶子包统一其 Respo 请求版本前，Caps 暂时使用常规的最高版本解析模式。
