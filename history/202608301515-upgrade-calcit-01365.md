# 2026-08-30 Upgrade Calcit 0.13.65 / 升级 Calcit 0.13.65

- Align the embedded Rust runtime, Calcit toolchain metadata, and JS procs on
  Calcit 0.13.65, including the WASM-safe crate dependency boundary.
- Pin the complete Calcit module graph to released tags and remove mutable
  `main` dependencies.
- Consume Memof 0.0.29 after its strict-macro compatibility release.
- Migrate deprecated pattern matching, retain explicit Option functions at
  Dynamic Respo/JS boundaries, and strengthen CI with strict Caps/toolchain
  and analysis checks.
- Install the Yarn PnP graph before verifying `@calcit/procs`, so clean CI
  runners can enforce toolchain lockstep.
- Make the keyboard event handler return an explicit Unit after its JS effect,
  eliminating the FFI nullish return warning.
- Instantiate empty map/list fallback values instead of passing collection
  constructor functions through `option:unwrap-or`.
- 将内嵌 Rust runtime、Calcit 工具链元数据与 JS procs 对齐到 0.13.65，并接入
  WASM 安全的 crate 依赖边界。
- 将完整模块依赖图固定到已发布 tag，移除可变的 `main` 依赖。
- 接入完成严格 Macro 兼容升级的 Memof 0.0.29。
- 迁移旧模式匹配；在 Dynamic Respo/JS 边界保留显式 Option 函数，并用严格
  Caps、工具链及静态分析检查强化 CI。
- 在校验 `@calcit/procs` 前先安装 Yarn PnP 依赖图，让干净 CI runner 可以执行
  工具链锁步守门。
- 键盘事件处理器在 JS effect 后显式返回 Unit，消除 FFI nullish 返回值告警。
- 将 `option:unwrap-or` 的空 Map/List 缺省值改为显式实例，避免误传集合构造函数。
